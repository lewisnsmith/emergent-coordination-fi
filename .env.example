# Copy to .env and fill in. Only needed for real-model runs (Phase B).
# The full pipeline runs end-to-end with mock models and no keys.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
# Generic OpenAI-compatible endpoint (Ollama, vLLM, OpenRouter, ...)
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=
# SEC EDGAR requires a descriptive User-Agent for 13F reference data
EDGAR_USER_AGENT="flock research <you@example.com>"
